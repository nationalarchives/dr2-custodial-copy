preservica-secret-name=${PRESERVICA_SECRET_NAME}
database-path=${DATABASE_PATH}
max-concurrency=50
max-concurrency=${?MAX_CONCURRENCY}
ocfl-repo-dir=${OCFL_REPO_DIR}
ocfl-work-dir=${OCFL_WORK_DIR}
proxy-url=${?HTTPS_PROXY}
days-to-ignore=1
days-to-ignore=${?DAYS_TO_IGNORE}
entities-updated-since-window-days=10
entities-updated-since-window-days=${?ENTITIES_UPDATED_SINCE_WINDOW_DAYS}
entities-updated-since-concurrency=20
entities-updated-since-concurrency=${?ENTITIES_UPDATED_SINCE_CONCURRENCY}
